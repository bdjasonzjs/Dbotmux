<title>Clone Integrity Gate E2E-3 Urgent Timeout Fix Review 1</title>

<callout emoji="✅" background-color="light-green" border-color="green">
  <p><b>结论：</b>本轮 review 通过，未发现新的 P0/P1。urgent 仍只从 clone 侧 <code>wake-ack</code> 判绿，Base record 写入成功不算通过。</p>
</callout>

<h1>Review Target</h1>
<table>
  <tbody>
    <tr><td>Reviewer</td><td>Codex 初号机</td></tr>
    <tr><td>Date</td><td>2026-06-20</td></tr>
    <tr><td>Input</td><td><code>/data00/home/zoujinsong.jason/work/Dbotmux_wt/clone-integrity-gate/docs/clone-integrity-gate-e2e3-urgent-timeout-fix-review-request.md</code></td></tr>
  </tbody>
</table>

<h1>Conclusion</h1>
<p>Pass for this review round. I did not find any new P0/P1 blocker.</p>
<p>The urgent path still marks green only from clone-side <code>wake-ack</code>; Base record write success is only evidence that the relay was queued. The wait is bounded at <code>PREHEAT_MAX_ATTEMPTS * PREHEAT_ATTEMPT_WINDOW_MS</code> = about 180 seconds, so this does not introduce an unbounded handoff stall.</p>

<h1>Checked Items</h1>
<ul>
  <li><code>src/services/ceo-preheat.ts</code></li>
  <li><code>src/services/ceo-spawn-service.ts</code></li>
  <li><code>src/im/lark/event-dispatcher.ts</code></li>
  <li><code>src/services/base-relay.ts</code></li>
  <li><code>test/ceo-preheat.test.ts</code></li>
</ul>

<hr/>

<h1>Findings</h1>
<callout emoji="✅" background-color="light-green" border-color="green">
  <p>No blocking findings.</p>
</callout>

<h2>P2 - Stale ceo-preheat comment still names sendAsOwner</h2>
<p><code>src/services/ceo-preheat.ts</code> top-level comment still says the retry must be a new record via direct <code>sendAsOwner</code>. The implementation now injects <code>writeRelayRecord()</code> from <code>ceo-spawn-service.ts</code>, which is the correct non-polling primitive for this fix.</p>
<p>This is not a behavior blocker. Clean it up before final polish so future maintainers do not reintroduce the short <code>sendAsOwner(... pollTimeoutMs: 3_000)</code> path.</p>

<h1>Review Notes</h1>
<ul>
  <li><code>preheatConfirmOnline()</code> records relay write start/success/failure, record IDs, elapsed time, and final ack/exhaustion.</li>
  <li><code>ceo-spawn-service.ts</code> now uses <code>writeRelayRecord()</code> for urgent probes and still returns <code>urgent_summon=pass</code> only when <code>preheatConfirmOnline()</code> observes <code>wake-ack</code>.</li>
  <li><code>event-dispatcher.ts</code> logs clone-side urgent wake-ack receipt with task, wake, message, chat, and sender evidence.</li>
  <li>Scope behavior remains fail-closed except for the existing empty-grant runtime-capability repair path; if urgent still fails, scope does not become green.</li>
</ul>

<h1>Verification Re-run</h1>
<table>
  <thead>
    <tr><th background-color="light-gray">Command</th><th background-color="light-gray">Result</th></tr>
  </thead>
  <tbody>
    <tr><td><code>pnpm vitest run test/ceo-preheat.test.ts test/clone-integrity-gate.test.ts test/event-dispatcher.test.ts test/ceo-clone-orchestration.test.ts test/ceo-spawn-wiring.test.ts test/clone-app-preset.test.ts test/bot-clone.test.ts test/clone-mention-resolver.test.ts test/base-relay.test.ts</code></td><td>9 test files passed; 258 tests passed</td></tr>
    <tr><td><code>pnpm exec tsc --noEmit</code></td><td>passed</td></tr>
    <tr><td><code>git diff --check</code></td><td>passed</td></tr>
  </tbody>
</table>

<h1>Residual Risk</h1>
<callout emoji="⚠️" background-color="light-yellow" border-color="yellow">
  <p>This review is still code/test review only. The live full clone E2E still needs to be rerun after deployment/restart authorization, with urgent evidence from both CEO-side relay/ack logs and clone-side wake-ack receipt logs.</p>
</callout>
